clear variables;
f = [0 0.2 0.25 0.45 0.5 1];
m = [0 0 1 1 0 0];
w = [0.2 0.25 0.45 0.5];

R_bt = 0.01; %riplul in banda de trecere
R_bo = 0.1; %riplul in banda de oprire
dev = [R_bo, R_bt, R_bo];
a = [0 1 0]; % amplitudini

[N, f0, a0, dev] = firpmord(w, a, dev);

h = firpm(N, f0, a0);
%raspuns la impuls
figure,
impz(h, 1), grid;

%raspuns la frecventa
figure, freqz(h,1,2^10);

%intarzirea de greup
figure, grpdelay(h,1,2^10);