clear variables;
N=36;%ordin
L=N+1;%lungime
Fc=15*10.^3;
Fe=5*10.^4;
fc=Fc/(Fe/2);%frecv normalizata

%proiectare filtru
h=fir1(N,fc);

%raspuns la impuls
figure, impz(h);

%raspuns la frecventa
figure, freqz(h,1,2^10);

%diagrama zerourilor
figure, zplane(h,1);